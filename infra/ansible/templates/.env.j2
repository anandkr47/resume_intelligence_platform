# Ansible-generated environment for {{ environment }}
# Update with your values in group_vars or host_vars

POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD={{ postgres_password | default('CHANGE_ME') }}
POSTGRES_DB=resume_platform

REDIS_HOST=redis
REDIS_PORT=6379

NODE_ENV=production
