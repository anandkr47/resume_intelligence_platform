{
  "name": "@resume-platform/queue-lib",
  "version": "1.0.0",
  "description": "Shared queue utilities for BullMQ",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "bullmq": "^5.1.0",
    "@resume-platform/config": "*",
    "@resume-platform/logger": "*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2"
  }
}
